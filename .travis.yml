language: php
php:
  - 7.4

cache:
  directories:
    - vendor
    - $HOME/.composer/cache

before_install:
  - sudo apt-get update > /dev/null

before_script:
  - composer install

jobs:
  include:
  - name: AppOne test
    before_script:
      - cd ./app
    script:
      - php ../vendor/bin/codecept run unit
  - name: AppTwo test
    before_script:
      - cd ./app2
    script:
      - php ../vendor/bin/codecept run unit

